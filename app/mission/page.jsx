'use client';import{useEffect,useMemo,useState}from'react';import{orbitEvents}from'../../lib/data';import{useSite}from'../../components/SiteProvider';import{Reveal}from'../../components/Reveal';export default function Mission(){const[running,setRunning]=useState(false),[angle,setAngle]=useState(0),[fuel,setFuel]=useState(92),[st,setSt]=useState(90),[msg,setMsg]=useState(orbitEvents[0]);const{spend}=useSite();useEffect(()=>{if(!running)return;let raf=0,last=performance.now();const loop=t=>{const dt=Math.min(40,t-last);last=t;setAngle(a=>(a+dt*.05)%360);raf=requestAnimationFrame(loop)};raf=requestAnimationFrame(loop);return()=>cancelAnimationFrame(raf)},[running]);useEffect(()=>{if(!running)return;const id=setInterval(()=>setMsg(orbitEvents[Math.floor(Math.random()*orbitEvents.length)]),2800);return()=>clearInterval(id)},[running]);const rad=angle*Math.PI/180;const pos=useMemo(()=>({left:`${50+Math.cos(rad)*40}%`,top:`${50+Math.sin(rad)*28}%`}),[angle]);const act=k=>{setMsg(orbitEvents[Math.floor(Math.random()*orbitEvents.length)]);if(k==='boost'){setFuel(v=>Math.max(0,v-7));setSt(v=>Math.max(0,v-4));spend(2)}else if(k==='brake')setSt(v=>Math.min(100,v+6));else{setAngle(a=>(a+70)%360);setFuel(v=>Math.max(0,v-2))}};return <div className="page"><div className="page-title"><span className="eyebrow">ROOM 03 / LYKA-01</span><h1>Actually <em>moving.</em></h1><p className="lede">Launch karo. Satellite live roam karega. Fuel aur stability ko sambhalo.</p></div><Reveal className="orbit-stage"><div className="orbit-stars"/><div className="orbit-ring"/><div className="orbit-planet"/><div className="satellite" style={pos}><span/></div><div className="glass orbit-panel"><span className="eyebrow">MISSION CONTROL</span><div className="telemetry-row"><span>FUEL</span><b>{fuel}%</b></div><div className="meter"><i style={{width:`${fuel}%`}}/></div><div className="telemetry-row"><span>STABILITY</span><b>{st}%</b></div><div className="meter"><i style={{width:`${st}%`}}/></div><div className="telemetry-row"><span>ANGLE</span><b>{Math.round(angle)}°</b></div><div className="mission-controls"><button className="button" onClick={()=>act('boost')}>BOOST</button><button className="button" onClick={()=>act('brake')}>BRAKE</button><button className="button" onClick={()=>act('reroute')}>REROUTE</button></div><button className="button hot orbit-launch" onClick={()=>setRunning(v=>!v)}>{running?'PAUSE ORBIT':'LAUNCH LYKA-01'}</button><div className="mission-log">{msg}</div></div><div className="glass orbit-objective"><span className="eyebrow">OBJECTIVE</span><h3>Keep Lyka alive.</h3><p>Fuel zero nahi. Stability crash nahi. Maths Sir ka transmission miss nahi.</p></div></Reveal></div>}
