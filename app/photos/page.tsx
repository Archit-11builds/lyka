"use client";
import {useState} from "react";
export default function Photos(){const [open,setOpen]=useState<string|null>(null);const pics=[['/photos/lyka-01.jpeg','FIELD IMAGE 01'],['/photos/lyka-02.jpeg','FIELD IMAGE 02']];return <div className="page"><header className="page-head"><span className="eyebrow">PHOTO ROOM / REAL EVIDENCE</span><h1>Actual photos.<br/><span>Fake science.</span></h1><p className="lede">Photo real hai. Investigation bilkul serious dikh rahi hai. Bas serious hai nahi.</p></header><div className="photo-grid">{pics.map(([src,label])=><button className="photo-card" key={src} onClick={()=>setOpen(src)}><img src={src} alt="Lyka field evidence"/><span>{label} ↗</span></button>)}</div>{open&&<div className="modal" onClick={()=>setOpen(null)}><div className="modal-card photo-modal" onClick={e=>e.stopPropagation()}><img src={open} alt="Expanded field evidence"/><button className="btn primary" onClick={()=>setOpen(null)}>Close</button></div></div>}</div>}
