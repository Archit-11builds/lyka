'use client';import{useEffect,useMemo,useState}from'react';import{maths,hardMaths}from'../../lib/data';import{Reveal}from'../../components/Reveal';const shuffle=a=>[...a].sort(()=>Math.random()-.5);export default function MathsSir(){const[mode,setMode]=useState('hard'),[i,setI]=useState(0),[sel,setSel]=useState(''),[reply,setReply]=useState(''),[score,setScore]=useState(0),[streak,setStreak]=useState(0),[seconds,setSeconds]=useState(45),[started,setStarted]=useState(false),[focus,setFocus]=useState('QUESTION');const bank=mode==='hard'?hardMaths:maths,q=bank[i%bank.length],opts=useMemo(()=>shuffle(q.opts),[q]);useEffect(()=>{if(!started||sel)return;if(seconds<=0){setSel('__timeout__');setReply('TIME. Maths Sir: period khatam.');setStreak(0);return}const id=setInterval(()=>setSeconds(s=>s-1),1000);return()=>clearInterval(id)},[started,seconds,sel]);const choose=v=>{if(sel)return;setStarted(true);setSel(v);if(v===q.a){setScore(s=>s+1);setStreak(s=>s+1);setReply(q.right)}else{setStreak(0);setReply(q.wrong)}};const reset=()=>{setSel('');setReply('');setSeconds(mode==='hard'?45:60);setStarted(false)};const next=()=>{setI(n=>(n+1)%bank.length);reset()};const changeMode=m=>{setMode(m);setI(0);setScore(0);setStreak(0);setSeconds(m==='hard'?45:60);setSel('');setReply('');setStarted(false)};const accuracy=score?Math.round((score/Math.max(1,i+(sel?1:0)))*100):0;return <div className="page maths-page"><div className="page-hero"><div className="page-title"><span className="eyebrow">ROOM 02 / MATHS SIR / TRAINING LAB</span><h1>Maths <em>Sir.</em></h1><p className="lede">A proper training interface: core practice, hard problems, streaks, timed runs and hidden modes.</p></div><div className="maths-top-stats"><div className="glass stat"><small>SCORE</small><b>{score}</b></div><div className="glass stat"><small>STREAK</small><b>{streak}</b></div><div className="glass stat"><small>ACCURACY</small><b>{accuracy}%</b></div></div></div><div className="maths-toolbar glass"><div><span className="eyebrow">DIFFICULTY</span><div className="mode-switch"><button className={mode==='normal'?'selected':''} onClick={()=>changeMode('normal')}>CORE</button><button className={mode==='hard'?'selected':''} onClick={()=>changeMode('hard')}>HARD LAB</button></div></div><div className="maths-focus">{['QUESTION','STREAK','SPEED'].map(x=><button key={x} className={focus===x?'active':''} onClick={()=>setFocus(x)}>{x}</button>)}</div><div className="timer-box"><span className="eyebrow">TIME</span><strong>{String(Math.floor(seconds/60)).padStart(2,'0')}:{String(seconds%60).padStart(2,'0')}</strong></div><button className="button" onClick={()=>{setI(Math.floor(Math.random()*bank.length));reset()}}>RANDOM ↻</button></div><Reveal className="maths-grid"><div className={'glass question maths-question '+(focus==='SPEED'?'speed-focus':'')}><div className="question-top"><span className="eyebrow">QUESTION {(i%bank.length)+1} / {bank.length}</span><span className="eyebrow">{mode==='hard'?'HARD LAB':'CORE'}</span></div><div className="meter"><i style={{width:`${(((i%bank.length)+1)/bank.length)*100}%`}}/></div><h2>{q.q}</h2><div className="options">{opts.map(v=><button key={v} disabled={!!sel} onClick={()=>choose(v)} className={sel===v?(v===q.a?'correct':'wrong'):''}>{v}</button>)}</div>{reply&&<div className="reply">{reply}</div>}<div className="question-actions"><button className="button hot" disabled={!sel} onClick={next}>NEXT QUESTION →</button><button className="button" onClick={reset}>RESET</button></div></div><div className="maths-side"><div className="glass mini"><span className="eyebrow">FOCUS / {focus}</span><h3>{focus==='QUESTION'?'Read first.':focus==='STREAK'?'Protect it.':'Beat the clock.'}</h3><p>Identify the chapter, write the givens, then calculate. Speed comes after accuracy.</p></div><div className="glass mini challenge"><span className="eyebrow">SESSION TARGET</span><h3>{mode==='hard'?'8 / 12':'6 / 8'}</h3><p>Hit the target without guessing. Wrong answer resets the streak.</p></div></div></Reveal></div>}