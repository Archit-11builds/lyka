"use client";
import {useState} from "react";import {jokes} from "@/lib/jokes";
export default function Archive(){const [open,setOpen]=useState<number|null>(null);const files=[...jokes,...jokes.slice(0,10)];return <div className="page"><header className="page-head"><span className="eyebrow">CLASSIFIED / FRIEND EVIDENCE</span><h1>Files jo <span>nahi mangi.</span></h1><p className="lede">Chhoti files. Bade confidence. Zero public demand.</p></header><div className="archive-grid">{files.map((j,i)=><button className="archive-card" key={i} onClick={()=>setOpen(i)}><small>FILE {String(i+1).padStart(2,"0")}</small><strong>{i%4===0?"CONFIDENCE REPORT":i%4===1?"FIELD OBSERVATION":i%4===2?"UNNECESSARY EVIDENCE":"INCIDENT LOG"}</strong><span>OPEN ↗</span></button>)}</div>{open!==null&&<div className="modal" onClick={()=>setOpen(null)}><div className="modal-card" onClick={e=>e.stopPropagation()}><span className="kicker">FILE {String(open+1).padStart(2,"0")}</span><h2>Evidence found.</h2><p className="modal-note">{files[open]}</p><button className="btn primary" onClick={()=>setOpen(null)}>Close file</button></div></div>}</div>}
