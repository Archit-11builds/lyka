'use client';import{useState}from'react';import{Reveal}from'../../components/Reveal';const f=[['ANUCHHED PROTOCOL','Anuradha → Anu → Anuched. Language department compromised.'],['MATHS PARADOX','Favourite subject + worst subject = Anik.'],['COOL INCIDENTS','Confidence events archived.'],['SPACE DOG THEORY','Human classification pending.'],['GHEE RESERVE','Resource management questionable.'],['SANKI SCIENTIST','Research continues without approval.']];export default function Archive(){const[filter,setFilter]=useState('ALL');const[active,setActive]=useState(null);const groups={ALL:f,LORE:f.slice(0,4),SYSTEM:f.slice(4),CHAOS:[f[1],f[2],f[5]]};const list=groups[filter];return <div className="page archive-page"><div className="page-title"><span className="eyebrow">THE ARCHIVE / MASTER INDEX</span><h1>Nothing here was <em>necessary.</em></h1><p className="lede">Search the private index. Some files are useful. Most are absolutely not.</p></div><div className="archive-filters glass">{Object.keys(groups).map(x=><button key={x} className={filter===x?'active':''} onClick={()=>setFilter(x)}>{x}</button>)}<span>{list.length} FILES</span></div><Reveal className="archive-grid archive-index-grid">{list.map(([t,p],i)=><article className={'glass archive-card '+(active===t?'active-file':'')} key={t} onClick={()=>setActive(active===t?null:t)}><span className="eyebrow">FILE {String(i+1).padStart(2,'0')}</span><h3>{t}</h3><p>{p}</p>{active===t&&<div className="file-expanded">ACCESS GRANTED / INDEXED 2026<br/>Origin: LYKA Field System<br/>Classification: unnecessary but important.</div>}</article>)}</Reveal></div>}