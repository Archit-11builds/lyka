'use client';import{useState}from'react';import{Reveal}from'../../components/Reveal';const events=['THE GREAT GHEE INCIDENT','THE MATHS SIR EVENT','THE UNNECESSARY CONFIDENCE REPORT','THE ARCHIVE DISCOVERY','THE BUTTON THAT SHOULD NOT HAVE BEEN PRESSED'];export default function Incidents(){const[n,setN]=useState(0);return <div className="page fun-page"><div className="page-title"><span className="eyebrow">ROOM 13 / INCIDENT ROOM</span><h1>Classified<br/><em>for no reason.</em></h1><p className="lede">Browse documented incidents from the LYKA field office.</p></div><Reveal className="incident-room"><div className="incident-index">{events.map((x,i)=><button className={i===n?'active':''} key={x} onClick={()=>setN(i)}>{String(i+1).padStart(2,'0')} / {x}</button>)}</div><div className="incident-detail"><small>CASE {String(n+1).padStart(2,'0')} / STATUS: ARCHIVED</small><h2>{events[n]}</h2><p>The evidence is incomplete, the explanation is unnecessary, and the archive has decided to preserve it anyway.</p><span>SEVERITY / UNREASONABLY HIGH</span></div></Reveal></div>}