"use client";
import { memeCards } from "@/lib/jokes"; import { useMemo,useState } from "react";
export default function Memes(){const [open,setOpen]=useState<number|null>(null); const cards=useMemo(()=>[...memeCards].sort(()=>Math.random()-.5),[]); return <div className="page"><header className="page-head"><span className="eyebrow">MEME VAULT / NO CONTEXT</span><h1>Hansi ka <span>archive.</span></h1><p className="lede">Cards kholo. Context mat maango. Meme samajh aaya toh theek, nahi aaya toh Anik ki galti.</p></header><div className="meme-wall">{cards.map((m,i)=><button key={i} className={`meme-card meme-${m[2]}`} onClick={()=>setOpen(i)}><span>{m[0]}</span><strong>{m[1]}</strong><small>OPEN ↗</small></button>)}</div>{open!==null&&<div className="modal" onClick={()=>setOpen(null)}><div className="modal-card" onClick={e=>e.stopPropagation()}><span className="kicker">MEME FILE</span><h2>{cards[open][0]}</h2><p className="modal-note">{cards[open][1]}</p><button className="btn primary" onClick={()=>setOpen(null)}>Theek hai bhai</button></div></div>}</div>}
