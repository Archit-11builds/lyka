'use client';import{useMemo,useState}from'react';import{jokes}from'../../lib/data';import{Reveal}from'../../components/Reveal';export default function Memes(){const[s,setS]=useState(0);const cards=useMemo(()=>Array.from({length:10},(_,i)=>jokes[(i+s)%jokes.length]),[s]);return <div className="page"><div className="page-hero"><div className="page-title"><span className="eyebrow">THE VAULT / MEMES</span><h1>Bad decisions,<br/><em>beautifully archived.</em></h1><p className="lede">Shuffle karo. Card kholo. Group chat mein bhejo.</p></div><button className="button hot" onClick={()=>setS(x=>x+1)}>SHUFFLE ↻</button></div><Reveal className="archive-grid">{cards.map((x,i)=><article className="glass archive-card" key={`${s}-${i}`}><span className="eyebrow">MEME {String(i+1).padStart(2,'0')}</span><h3>{x}</h3><p>Evidence quality: suspiciously high.</p></article>)}</Reveal></div>}
