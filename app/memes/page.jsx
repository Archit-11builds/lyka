'use client';import{useMemo,useState}from'react';import{jokes}from'../../lib/data';import{Reveal}from'../../components/Reveal';export default function Memes(){const[s,setS]=useState(0);const[open,setOpen]=useState(null);const[view,setView]=useState('GRID');const cards=useMemo(()=>Array.from({length:10},(_,i)=>jokes[(i+s)%jokes.length]),[s]);return <div className="page memes-page"><div className="page-hero"><div className="page-title"><span className="eyebrow">THE VAULT / MEMES</span><h1>Bad decisions,<br/><em>beautifully archived.</em></h1><p className="lede">Shuffle, inspect, and open the files the archive refuses to take seriously.</p></div><div className="vault-actions"><button className="button hot" onClick={()=>setS(x=>x+1)}>SHUFFLE ↻</button><button className="button" onClick={()=>setView(view==='GRID'?'INDEX':'GRID')}>{view==='GRID'?'INDEX VIEW':'GRID VIEW'}</button></div></div><Reveal className={'archive-grid '+(view==='INDEX'?'index-view':'')}>{cards.map((x,i)=><article className={'glass archive-card '+(open===i?'opened':'')} key={`${s}-${i}`} onClick={()=>setOpen(open===i?null:i)}><div className="meme-top"><span className="eyebrow">MEME {String(i+1).padStart(2,'0')}</span><b>{open===i?'−':'+'}</b></div><div className="meme-number">0{i+1}</div><h3>{x}</h3><p>{open===i?'ARCHIVE NOTE: Evidence quality confirmed suspiciously high.':'Click to inspect file.'}</p></article>)}</Reveal></div>}