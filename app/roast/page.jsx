'use client';import{useState}from'react';import{useSite}from'../../components/SiteProvider';import{jokes}from'../../lib/data';import{Reveal}from'../../components/Reveal';export default function Roast(){const{cool,coolUp,ghee}=useSite();const[line,setLine]=useState(jokes[0]);const[heat,setHeat]=useState(0);const[secret,setSecret]=useState(false);const fire=()=>{coolUp();setHeat(v=>Math.min(100,v+12));setLine(jokes[Math.floor(Math.random()*jokes.length)])};return <div className="page roast-page"><div className="page-title"><span className="eyebrow">ROOM 01 / ROAST LAB</span><h1>Ban Gaya <em>Cool.</em></h1><p className="lede">A ridiculous confidence simulator. Every click changes the atmosphere.</p></div><Reveal className="roast-layout"><div className="glass roast-main"><div className="roast-radar"><i/></div><span className="eyebrow">COOL EVENTS</span><div className="cool-count">×{cool}</div><button className="button hot roast-trigger" onClick={fire}>BAN GAYA COOL <b>↗</b></button><div className="meter"><i style={{width:`${Math.min(100,8+cool*7)}%`}}/></div><div className="heat-track"><span>CHAOS</span><i style={{width:heat+'%'}}/></div><p className="verdict">“{line}”</p>{cool>=6&&<button className="secret-trigger" onClick={()=>setSecret(v=>!v)}>{secret?'CLOSE SECRET':'▣ UNLOCK LEVEL 2'}</button>}{secret&&<div className="roast-secret">LEVEL 2 / DELUSION ENGINE<br/><strong>CONFIDENCE DETECTED.</strong><br/>Scientific conclusion: absolutely unnecessary.</div>}</div><div className="roast-side"><div className="glass mini"><span className="eyebrow">REALITY</span><h3>{Math.max(2,100-Math.min(98,cool*7))}%</h3><p>Cool count upar. Reality thoda neeche.</p></div><div className="glass mini"><span className="eyebrow">GHEE</span><h3>{ghee}%</h3><p>Every cool move has a cost.</p></div><div className="glass mini roast-tip"><span className="eyebrow">LAB RULE</span><h3>Keep clicking.</h3><p>There is no meaningful scientific reason to stop.</p></div></div></Reveal></div>}