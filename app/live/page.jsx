'use client';import{useEffect,useState}from'react';import{Reveal}from'../../components/Reveal';const states=['SUBJECT MOVING','GHEE DETECTED','MATHS SIGNAL ACTIVE','ARCHIVE EXPANDING','UNKNOWN ACTIVITY','SYSTEM CALM (SUSPICIOUS)'];export default function Live(){const[s,setS]=useState(states[0]),[events,setEvents]=useState([]);useEffect(()=>{const id=setInterval(()=>{const next=states[Math.floor(Math.random()*states.length)];setS(next);setEvents(v=>[{time:new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit',second:'2-digit'}),text:next},...v].slice(0,8))},1200);return()=>clearInterval(id)},[]);return <div className="page fun-page"><div className="page-title"><span className="eyebrow">ROOM 12 / LIVE FEED</span><h1>LYKA<br/><em>is watching.</em></h1><p className="lede">Fake telemetry. Real entertainment.</p></div><Reveal className="live-feed"><div className="live-status"><i/> LIVE / {s}</div><div className="feed-grid">{Array.from({length:8},(_,i)=><article key={i}><span>0{i+1}:32:0{i}</span><b>{states[(i+2)%states.length]}</b><small>FIELD SENSOR / ONLINE</small></article>)}</div></Reveal><section className="live-event-stream glass"><div><span>LIVE EVENT STREAM</span><b>{events.length.toString().padStart(2,'0')} SIGNALS</b></div>{events.map((x,i)=><article key={i}><small>{x.time}</small><span>{x.text}</span><i>ONLINE</i></article>)}{!events.length&&<p>Listening for the next field signal…</p>}</section></div>}